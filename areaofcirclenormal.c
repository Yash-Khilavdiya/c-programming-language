#include<stdio.h>

int main(){
    int radius=4;
    float pi=3.14;
    float area=pi*radius*radius;
    printf("the area of circle is %f", area);
    return 0;
}
//here instead of using second float we can directly write pi*radius*radius instead of area